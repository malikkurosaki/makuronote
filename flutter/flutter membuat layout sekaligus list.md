# fllutter membut layout sekaligus

```dart
children: List.generate(4, (index){
                      return Center(
                        child: Text("apa kabar"),
                      );
                    }),
```
