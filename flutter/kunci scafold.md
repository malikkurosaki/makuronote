# kunci scafold

```dart

GlobalKey<ScaffoldState> _kunciScafold = GlobalKey<ScaffoldState>();

// terapan

return new Scaffold(
      key: _kunciScafold,
      drawer: drawerNya(context),
      backgroundColor: Colors.blueGrey[50],
      body: new SafeArea(
        child:
```
