```bash
echo -e "
            -r, run                 Run project
            -e, emulator            Run emulator
            -l, launch              Launch project
            -i, info                Show info about project
            -m, mode                Change mode
            -g, git                 Git operation
            -lgn, login             Login to report-mobile-dev.probussystem.net
            " | sed -e 's/.*[-]/-/'
```
